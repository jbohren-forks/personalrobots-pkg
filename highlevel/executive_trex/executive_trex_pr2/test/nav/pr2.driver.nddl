// This simple test will execute three navigation goals and we should expect that
// the base controller is only loaded once
#include "nav/imports.nddl"

close();

rejectable(move_base.Active g0);
g0.x = 19.5;
g0.y = 28.5;
g0.th= 0.5;

rejectable(move_base.Active g1);
g1.x = 15.0;
g1.y = 25.2;
g1.th = 0.5;

rejectable(move_base.Active g2);
g2.x = 10.6;
g2.y = 27.15;
g2.th = 1.0;