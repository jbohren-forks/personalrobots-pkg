#include "doors/doorman.nddl"

DoorController door_controller = new DoorController(Internal, false);

close();


// Loads the map from file
map_initialize_from_file("../test/willow.tmap");

// Will be trying to get thru a doorway which will exity at positin <3, 6, 2.0>
rejectable(door_controller.Holds g0);
g0.target_x = 3.0;
g0.target_y = 6.0;
g0.target_th = 2.0;