/**
 * Execute a single external action
 */
#include "TREX.nddl"
#include "rcs/exports.nddl"
#include "rcs/mechanism_control/exports.nddl"

Behavior::Active{
  addEq(dispatch_time, [0, 1], start);
  eq(status, SUCCESS);
}

// Swap Command
SwitchControllers switch_controllers = new SwitchControllers(Internal);

close();