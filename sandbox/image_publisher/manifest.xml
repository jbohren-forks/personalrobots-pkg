<package>
  <description brief="image_publisher">

     image_publisher provides facilities for transmitting images efficiently over a bandwidth-limited network connection. These facilities publish low-bandwidth versions of a raw image on parallel topics.

  </description>
  <author>Patrick Mihelich/ mihelich@willowgarage.com</author>
  <license>BSD</license>
  <review status="proposal cleared" notes=""/>
  <url>http://ros.org/wiki/image_publisher</url>

  <depend package="roscpp"/>
  <depend package="sensor_msgs"/>
  <depend package="opencv_latest"/>

  <export>
    <cpp lflags="-Wl,-rpath,${prefix}/lib -L${prefix}/lib -limage_publisher" cflags="-I${prefix}/include"/>
  </export>

</package>
