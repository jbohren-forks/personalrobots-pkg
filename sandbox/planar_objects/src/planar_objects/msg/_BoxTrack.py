# autogenerated by genmsg_py from BoxTrack.msg. Do not edit.
import roslib.message
import struct

import robot_msgs.msg
import planar_objects.msg
## \htmlinclude BoxTrack.msg.html

class BoxTrack(roslib.message.Message):
  _md5sum = "0d410ba4b73f2056cd3a7b5d6ef730a4"
  _type = "planar_objects/BoxTrack"
  _has_header = False #flag to mark the presence of a Header object
  _full_text = """int32 id
BoxObservation[] obs

float32 translation_tolerance
float32 rotation_tolerance
float32 size_tolerance
================================================================================
MSG: planar_objects/BoxObservation
robot_msgs/Transform transform
int32 plane_id

float64 w # width
float64 h # height 

float64 precision # fill ratio of rectangle
float64 recall # coverage ratio of rectangle in plane



================================================================================
MSG: robot_msgs/Transform
Vector3 translation
Quaternion rotation

================================================================================
MSG: robot_msgs/Vector3
float64 x
float64 y
float64 z
================================================================================
MSG: robot_msgs/Quaternion
# xyz - vector rotation axis, w - scalar term (cos(ang/2))
float64 x
float64 y
float64 z
float64 w

"""
  __slots__ = ['id','obs','translation_tolerance','rotation_tolerance','size_tolerance']
  _slot_types = ['int32','BoxObservation[]','float32','float32','float32']

  ## Constructor. Any message fields that are implicitly/explicitly
  ## set to None will be assigned a default value. The recommend
  ## use is keyword arguments as this is more robust to future message
  ## changes.  You cannot mix in-order arguments and keyword arguments.
  ##
  ## The available fields are:
  ##   id,obs,translation_tolerance,rotation_tolerance,size_tolerance
  ##
  ## @param args: complete set of field values, in .msg order
  ## @param kwds: use keyword arguments corresponding to message field names
  ## to set specific fields. 
  def __init__(self, *args, **kwds):
    super(BoxTrack, self).__init__(*args, **kwds)
    #message fields cannot be None, assign default values for those that are
    if self.id is None:
      self.id = 0
    if self.obs is None:
      self.obs = []
    if self.translation_tolerance is None:
      self.translation_tolerance = 0.
    if self.rotation_tolerance is None:
      self.rotation_tolerance = 0.
    if self.size_tolerance is None:
      self.size_tolerance = 0.

  ## internal API method
  def _get_types(self): return BoxTrack._slot_types

  ## serialize message into buffer
  ## @param buff StringIO: buffer
  def serialize(self, buff):
    try:
      buff.write(struct.pack('<i', self.id))
      #serialize self.obs
      length = len(self.obs)
      buff.write(struct.pack('<I', length))
      for val1 in self.obs:
        buff.write(struct.pack('<3d', val1.transform.translation.x, val1.transform.translation.y, val1.transform.translation.z))
        buff.write(struct.pack('<4d', val1.transform.rotation.x, val1.transform.rotation.y, val1.transform.rotation.z, val1.transform.rotation.w))
        buff.write(struct.pack('<i4d', val1.plane_id, val1.w, val1.h, val1.precision, val1.recall))
      buff.write(struct.pack('<3f', self.translation_tolerance, self.rotation_tolerance, self.size_tolerance))
    except struct.error, se: self._check_types(se)
    except TypeError, te: self._check_types(te)

  ## unpack serialized message in str into this message instance
  ## @param str str: byte array of serialized message
  def deserialize(self, str):
    try:
      end = 0
      start = end
      end += 4
      (self.id,) = struct.unpack('<i',str[start:end])
      #deserialize self.obs
      start = end
      end += 4
      (length,) = struct.unpack('<I',str[start:end])
      self.obs = []
      for i in xrange(0, length):
        val1 = planar_objects.msg.BoxObservation()
        start = end
        end += 24
        (val1.transform.translation.x, val1.transform.translation.y, val1.transform.translation.z,) = struct.unpack('<3d',str[start:end])
        start = end
        end += 32
        (val1.transform.rotation.x, val1.transform.rotation.y, val1.transform.rotation.z, val1.transform.rotation.w,) = struct.unpack('<4d',str[start:end])
        start = end
        end += 36
        (val1.plane_id, val1.w, val1.h, val1.precision, val1.recall,) = struct.unpack('<i4d',str[start:end])
        self.obs.append(val1)
      start = end
      end += 12
      (self.translation_tolerance, self.rotation_tolerance, self.size_tolerance,) = struct.unpack('<3f',str[start:end])
      return self
    except struct.error, e:
      raise roslib.message.DeserializationError(e) #most likely buffer underfill


  ## serialize message with numpy array types into buffer
  ## @param buff StringIO: buffer
  ## @param numpy module: numpy python module
  def serialize_numpy(self, buff, numpy):
    try:
      buff.write(struct.pack('<i', self.id))
      #serialize self.obs
      length = len(self.obs)
      buff.write(struct.pack('<I', length))
      for val1 in self.obs:
        buff.write(struct.pack('<3d', val1.transform.translation.x, val1.transform.translation.y, val1.transform.translation.z))
        buff.write(struct.pack('<4d', val1.transform.rotation.x, val1.transform.rotation.y, val1.transform.rotation.z, val1.transform.rotation.w))
        buff.write(struct.pack('<i4d', val1.plane_id, val1.w, val1.h, val1.precision, val1.recall))
      buff.write(struct.pack('<3f', self.translation_tolerance, self.rotation_tolerance, self.size_tolerance))
    except struct.error, se: self._check_types(se)
    except TypeError, te: self._check_types(te)

  ## unpack serialized message in str into this message instance using numpy for array types
  ## @param str str: byte array of serialized message
  ## @param numpy module: numpy python module
  def deserialize_numpy(self, str, numpy):
    try:
      end = 0
      start = end
      end += 4
      (self.id,) = struct.unpack('<i',str[start:end])
      #deserialize self.obs
      start = end
      end += 4
      (length,) = struct.unpack('<I',str[start:end])
      self.obs = []
      for i in xrange(0, length):
        val1 = planar_objects.msg.BoxObservation()
        start = end
        end += 24
        (val1.transform.translation.x, val1.transform.translation.y, val1.transform.translation.z,) = struct.unpack('<3d',str[start:end])
        start = end
        end += 32
        (val1.transform.rotation.x, val1.transform.rotation.y, val1.transform.rotation.z, val1.transform.rotation.w,) = struct.unpack('<4d',str[start:end])
        start = end
        end += 36
        (val1.plane_id, val1.w, val1.h, val1.precision, val1.recall,) = struct.unpack('<i4d',str[start:end])
        self.obs.append(val1)
      start = end
      end += 12
      (self.translation_tolerance, self.rotation_tolerance, self.size_tolerance,) = struct.unpack('<3f',str[start:end])
      return self
    except struct.error, e:
      raise roslib.message.DeserializationError(e) #most likely buffer underfill

