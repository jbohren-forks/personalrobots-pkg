# The definition of a kinematic path. A list of states, the
# joint names the states correspond to and the times at which 
# each state should be along the path

string[] names
KinematicState[] states
float64[] times
