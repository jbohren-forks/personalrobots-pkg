<package>
  <description>Scripts for checking the optical flag values. It is quite possible that all these scripts should be lumped into the kinematic calibration package.</description>
  <author>Vijay Pradeep</author>
  <license>BSD</license>
  <review status="experimental" notes=""/>
  <!-- depend package="kdl" / -->
  <!-- depend package="phase_space" / -->
  <depend package="robot_msgs" />
  <depend package="std_msgs" />
  <!-- depend package="robot_kinematics" / -->
  <!-- depend package="topic_synchronizer" / -->
  <!-- depend package="image_msgs" / -->
  <!-- depend package="newmat10"/ -->
  <depend package="pr2_mechanism_controllers" />

  <export>
    <cpp cflags="-I${prefix}/msg/cpp"/>
  </export>
</package>
