Index: stacks/simulators/gazebo/gazebo_user_cam_fix.diff
===================================================================
--- stacks/simulators/gazebo/gazebo_user_cam_fix.diff (revision 0)
+++ stacks/simulators/gazebo/gazebo_user_cam_fix.diff (revision 0)
@@ -0,0 +1,24 @@
+Index: server/rendering/UserCamera.cc
+===================================================================
+--- server/rendering/UserCamera.cc      (revision 7891)
++++ server/rendering/UserCamera.cc      (working copy)
+@@ -41,7 +41,8 @@
+ ////////////////////////////////////////////////////////////////////////////////
+ /// Constructor
+ UserCamera::UserCamera(GLWindow *parentWindow)
+-  : OgreCamera("UserCamera")
++  : OgreCamera("UserCamera"),
++    viewport( 0 )
+ {
+   std::stringstream stream;
+ 
+@@ -126,7 +127,8 @@
+ {
+   this->window->resize(w, h);
+   this->window->windowMovedOrResized();
+-  this->viewport->setDimensions(0,0,1,1);
++  if( viewport )
++      this->viewport->setDimensions(0,0,1,1);
+ }
+ 
+ ////////////////////////////////////////////////////////////////////////////////
