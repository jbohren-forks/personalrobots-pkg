cmake_minimum_required(VERSION 2.6)
include(rosbuild)
rospack(filters)
rospack_add_library(median_filter src/median.cpp )
rospack_add_gtest_future(median_test test/test_median.cpp)
target_link_libraries(median_test median_filter)