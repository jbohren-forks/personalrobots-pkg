<package>
  <description>
    A standalone hokuyo driver pulled from player distribution and
  modified heavily.  Once this driver is merged back into player, this
  package should be removed to avoid having parallel code.
  </description>
  <author>Jeremy Leibs</author>
  <license>LGPL</license>
  <export>
    <cpp cflags="-I${prefix}" lflags="-Wl,-rpath,${prefix}/lib -L${prefix}/lib -lurg"/>
  </export>
</package>
