Library dependency:
At the minimum, the visual odometry lib depends on 
opencv_latest
boost

JD is builting it out of its own local copy. We shall figure out the way
to use the ros copies.


Input Data directory:
/u/konolige/vslam/data/indoor1
You need to create a directory call Data at the same level as Debug, and within
Data, a symlink called indoor1 to the above directory.

Output Data directory:
By default the executable dumps stuff in 
Output/indoor1
so please make sure it exists

Then the following command line will start the program

Debug/PoseEstimationTest video > output.txt 2>&1